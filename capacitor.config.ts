import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PC3-test1-Miguel',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
